import { VoiceJournal } from '@/components/voice/VoiceJournal';

export const metadata = {
  title: 'Diario por Voz — Kairos',
};

export default function VoiceJournalPage() {
  return <VoiceJournal />;
}
